# Arquivo - Plano L1/L2/L3 De 2026-05-15

Este diretorio preserva o planejamento concluido da implementacao documental
L1/L2/L3 do `dex-memoria`.

Arquivos arquivados:

- `SPEC-PLAN.md`
- `SPRINTS.md`
- `TASKS.md`

Motivo do arquivo:

- o plano foi concluido e nao e mais fonte viva de execucao;
- o contrato atual vive em `SPEC.md`, `SKILL.md`, `README.md`, `docs/`,
  `templates/`, `examples/` e `scripts/validate-public.js`;
- manter estes arquivos no root fazia parecer que havia sprint/backlog ativo
  antigo.

Quando consultar:

- ao auditar a origem historica da arquitetura `L1 lembranca -> L2 memoria ->
  L3 conhecimento`;
- ao investigar decisoes de maio de 2026 antes das versoes `0.1.3+`.

Quando nao usar:

- para decidir o estado atual do pacote;
- para validar release atual;
- como backlog vivo.
